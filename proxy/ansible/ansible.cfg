[defaults]
cow_selection = unipony-smaller
inventory = ./hosts
host_key_checking = False
forks = 20
ansible_managed = Managed by Ansible - file:{file} - host:{host} - uid:{uid}
# not being respected (mainly collections)
#roles_path = ../../roles
#collections_paths = ../../collections
vault_password_file = ~/.config/base-docker/pass.txt

