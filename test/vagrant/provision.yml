---
- name: base setup
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: update apt cache
      apt:
        update_cache: true
        cache_valid_time: 3600

    - name: install packages
      apt:
        name:
          - git
          - curl
        state: present
